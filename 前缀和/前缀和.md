例如 p=[2,3,3,5]，对应的前缀和数组为 s=[0,2,5,8,13]。
 p 的子数组 [3,3,5] 的和就可以用 s[4]−s[1]=13−2=11 算出来。

 核心逻辑就是构建一个前缀和数组
 ```python
 prex = [0] * (n+1) // 注意是n+1 因为prex[0] = 0
 for i in range(n):
    prex[i+1] = prex[i] + nums[i]
这样通过一次遍历就能构建前缀和数组，需要某个区间的和只需要用prex[j] - prex[i]即可
    